/* ==========================================================
   METRA – AuditPanel Placeholder
   ----------------------------------------------------------
   Temporary stub for layout verification during Phase 4.3B.
   Replace later with the real Audit Panel component.
   ========================================================== */
import React from "react";

export default function AuditPanel() {
  return (
    <div
      style={{
        background: "#fff",
        border: "1px solid #ccc",
        padding: "1rem",
        borderRadius: "8px",
      }}
    >
      <h2 style={{ margin: "0 0 0.5rem 0" }}>Audit Panel (Placeholder)</h2>
      <p style={{ margin: 0 }}>
        This section remains visible for all roles to confirm layout.
      </p>
    </div>
  );
}
